# Local dev (Vite proxies /api to backend — see vite.config.js)
VITE_API_URL=/api

# Production (Coolify runtime): set VITE_API_URL or API_URL when the container starts
# VITE_API_URL=https://dt-server.wonderprotect.net/api
